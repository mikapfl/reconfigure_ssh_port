---
galaxy_info:
  role_name: reconfigure_ssh_port
  namespace: mikapfl
  author: Mika Pflüger
  description: Configure another ssh port without losing the ansible connection.
  company: Potsdam Institute for Climate Impact Research (PIK) e. V.

  license: Apache-2.0

  min_ansible_version: 2.1

  # To view available platforms and versions (or releases), visit:
  # https://galaxy.ansible.com/api/v1/platforms/
  #
  platforms:
    - name: opensuse
      versions:
        - 15.3
    - name: debian
      versions:
        - bullseye

  galaxy_tags:
    - ssh

collections:
  - community.general
